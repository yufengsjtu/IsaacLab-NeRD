Added
^^^^^

* Added reusable solver-active contact views so Active15 can share Raw15
  datasets, while shared-per-body and global token encoders can share one
  contact-token dataset with an aligned solver-active mask.
